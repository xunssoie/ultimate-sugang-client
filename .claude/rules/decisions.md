# 규칙 — 확정 결정(Locked Decisions)

> spec 간 충돌·미확정 사항에 대해 **이미 내려진 결정**. 재논의 금지(바꾸려면 사용자 승인). 근거 spec 섹션 또는 사용자 결정 날짜를 함께 적는다.
> **harness-init** 이 초기 결정(기본값 채택 내역)을 D1~ 로 기록하고, 이후 빌드 중 사용자 결정이 나올 때마다 오케스트레이터가 이어서 추가한다.

## 작성 형식 (항목당)
```
## D{n} — {한 줄 제목}
- 결정 내용(무엇이 확정인지, 무엇이 금지인지).
- 적용 화면/영역.
- 근거: spec §번호 또는 "사용자 결정 YYYY-MM-DD".
```

<!-- PROJECT:BEGIN — harness-init 부터 여기에 D1~ 를 기록한다. -->
## D1 — 백엔드 계약 미제공 → 어댑터 인터페이스 + mock 어댑터 기본
- 데이터 접근은 `features/sukang/api.ts` → `SukangApi` 어댑터(`03 §3-2`). 기본 `VITE_API_ADAPTER=mock`(인메모리 픽스처·200~800ms 지연·서버 검증 시뮬레이션). **엔드포인트/URL/envelope 창작 금지.** `http` 어댑터는 계약(Q-1) 도착 전 스텁만.
- 적용: 전 화면. 근거: 원§0 "서버·API 별도 존재" + 원부록 "엔드포인트 미조사" · 03 §2 N-1 · Q-1(승인 대상).

## D2 — shadcn/ui·sonner 미도입, 원본 CSS 이식 + 시맨틱 HTML + 네이티브 다이얼로그
- 원§6 CSS 를 `globals.css` 에 토큰 var() 로 치환 이식(`04 §4`), 컴포넌트는 원본 클래스명 사용. `<table>`·네이티브 `<select>`·`<button>`. `window.alert`/`confirm` 만(토스트·커스텀 모달 금지). `<font>` → `span.th-en`/`span.tag`. `#login`·`#noti_pop` id 선택자는 원문 유지(아키텍처 규칙 예외).
- 적용: 전 화면. 근거: 원§0 재현 대상(DOM·CSS) · 원§9.2 주석 · 03 §2 N-2. `rules/ui-conventions.md` "shadcn 우선" 은 이 프로젝트에서 이 결정으로 대체.

## D3 — CAPTCHA 미구현
- 신청 시 CAPTCHA 모달 없음. 원문은 `01 부록 A` 보존. 향후 활성화는 사용자 결정.
- 근거: 원§9.1 "넣지 않는 것을 권장" · Q-4.

## D4 — 원본 UX 결함 3건 그대로 재현
- ① 신청 성공 후 조회 목록 미갱신(버튼 `신청` 유지, `courses` 키 invalidate 금지) ② 결과 0건 시 헤더만·안내 문구 없음 ③ 정원/신청인원 컬럼 비노출(`마감`/`신청` 이진).
- 근거: 원§10.3·원§12 "결정 필요" → 연습 체감 동일을 위해 원본 유지 · Q-3.

## D5 — 원문 오탈자·띄어쓰기 그대로 재현
- `registrered`, `가능 합니다`, `신청 하였습니다`, (부록) `잘 못`. 근거: 원§8.7·§9.2 "재현 여부 선택" → 재현.

## D6 — 메시지 카탈로그: 원§9.3 제안 채택 + 하네스 fallback 2건
- `CREDIT_EXCEEDED`·`CLASS_FULL`·`SESSION_EXPIRED`·`NOT_IN_PERIOD` = 원§9.3 문구. 하네스 작성: `TIMEOUT` `요청 시간이 초과되었습니다. 다시 시도하세요.\n Request timed out.` · 알 수 없는 오류 `요청 처리 중 오류가 발생했습니다.\n Request failed.` 포맷 `한글\n 영문` 고정.
- 근거: 원§9.2 포맷 규칙 · 03 §2 N-6 · Q-13.

## D7 — iframe 미재현, 단일 SPA, 라우트 `/`(LOGIN) · `/sukang?menu={Key}`
- 근거: 원§1 주석 "단일 페이지로 만들어도 시각적 결과 동일" · 03 §2 N-7.

## D8 — 뷰포트/테마: 데스크탑(min-width 1400px 래퍼, 80% 폭)·라이트 전용
- 반응형 breakpoint·미디어쿼리·`dark:` 금지. 근거: 원§6 `.wrap` · 원§1 Chrome/Edge · 03 §2 N-8.

## D9 — 4상태 렌더 정책(원본 시각 유지)
- Loading = thead 만 + `aria-busy` + [조회] disabled(시각 변화 없음) · Error = `alert` 1회 + thead 만 · Empty = thead 만(안내 없음) · Data = 행. 진행 중 재클릭은 로직 가드만(disabled 스타일 없음).
- 근거: 원본에 로딩/빈 상태 시각 없음(원§10.3) · 하네스 4상태 요구 절충 · 03 §2 N-9.

## D10 — 컬럼 폭은 화면 설정 데이터(토큰 아님)
- 원§3 폭(50/70/80/543/50/65/250/125/80/65, 합 1378)을 `features/sukang/columns.ts` 에 숫자로 보관, `<colgroup>` 로 적용. 명세 밖 컬럼 폭(이수영역 125·순번 50·재수강 80·취소 65)과 "교과목명 = 1378 − 나머지" 규칙은 init 도출(`05 §4-5`).
- 근거: 원§3 "컬럼 정의를 화면별 설정값으로 주입" · 03 §2 N-10.

## D11 — 연습용 배너: `.wrap` 최상단 1줄(로그인은 박스 하단), `.noti em` 스타일
- 문구 `⚠️ 비공식 연습용 사이트 — 실제 수강신청과 무관합니다`. 근거: 원§0 필수 3(위치·스타일 미기재) · Q-10.

## D12 — 로고 텍스트 플레이스홀더 `[LOGO]` + 앱 명칭 `수강신청 연습 사이트 (비공식 Mock)`
- 학교 CI·"인천대학교" 표기 금지. `<title>` 동일. 근거: 원§0 필수 2 · 원§8.1 권장 변경 · Q-10.

## D13 — 토큰 명명: 원§7 이름 그대로 + 원§6/§8.6/§5.5 보충 토큰, Tailwind 별칭 `body`/`line`
- 보충: `--white` `--border-sch` `--border-input` `--grey-sch` `--noti-blue` `--bg-noti` `--tag-blue` `--time-th/sun/sat`. `.timeT th` 상단선 `#3D75BB` 는 `--blue-topline` 으로 통일. 새 토큰 추가는 spec 개정 없이는 금지.
- 근거: `04 §1-2` · 03 §2 N-13.

## D14 — 세션 = 표시용 학번 1개(Zustand + sessionStorage). 인증 없음
- 미로그인 `/sukang` → `/` 리다이렉트. `SESSION_EXPIRED` → alert → logout → `/`. 토큰/쿠키/재발급 없음.
- 근거: 원§0 "인증/세션 재현 안 함" · 원§12 세션 만료 · 03 §2 N-14 · Q-13.

## D15 — 이탈 보호 미적용 (편집/dirty 화면 없음)
- 근거: 03 §2 N-15.

## D16 — 서버 시간: `Date` 헤더 오프셋 유틸만, 표시 UI 없음
- `shared/api/serverTime.ts`. UI 는 Q-13 답변 시. 근거: 원§12 · 03 §2 N-16.

## D17 — 탭 활성 강조·행 hover 하이라이트 미구현
- 근거: 원§6 CSS 에 활성/hover 규칙 없음, 색 값 미확보 · Q-12.

## D18 — 폰트 스택 원문 그대로, 웹폰트 미도입
- `Dotum, Gulim, 돋움, 굴림, arial, sans-serif`. 근거: 원§7 · 라이선스 불명 · Q-15.

## D19 — 미기재 동작 기본값: 조건 미충족 조회 no-op · 빈 학번 진입 불가(메시지 없음) · 링크/바로가기 렌더만
- 교양 이수구분 미선택 / 타학과·연계 placeholder / 과목명 2자 미만 → 요청 안 보냄·메시지 없음. 로그인 빈 값 → 진입 안 함. `찾기` 링크·바로가기 4개 → `href` 없이 렌더.
- 근거: 원§8.1·§8.6 문구만 존재 · Q-7·Q-9.

## D20 — 초기 탭 `Basket`(첫 탭). `?menu` 무효값도 `Basket`
- 근거: 원§2.1 표 순서 · Q-8.

## D21 — 헤더 en 라벨 누락 컬럼은 한글만: `요일 및 교시(강의실)`, 액션 헤더 `신청`/`취소`
- 근거: 원§3 매트릭스 · Q-11.

## D22 — 시드 데이터: 명세 예시 행 + 화면당 소량 픽스처, 학생 = 명세 예시
- 강좌: `현장교육.실습(Ⅴ-1)`(0006154050, 12학점, 월 1~9 ZZ-102 ×5일) · `대학수학(2) [75분수업]` · `게임프로그래밍` IAA6066001(화 2 3, 목 5 6)/IAA6066002(수 5 6, 목 7 8) · `경영프로그래밍2`(0012410001, 경영학부, 전공심화→일반선택) + 마감 1·야간 1 포함. 학생: `컴퓨터공학부 / 홍○○ / 4 / 유예`, 학번 = 입력값. 초기 신청내역 1건(취소 검증용).
- mock 전용. 실 데이터는 Q-2. 근거: 원§5.4·§5.5·§10.1·§10.2 예시 · 03 §2 N-22.
## D3 (2026-09-06 변경 → D34) · D4 (→ D33 재확인) · D11 (→ **D52 D3-02 폐기**) · D18 (→ D36 → **D50 대체**) · D20 (→ D35 대체) · D22 (→ D26·D43 대체)

## D23 — 확인서출력·시간표출력: 화면 미구현, 버튼 렌더 + alert (Q-5)
- 버튼은 원본 시각 그대로(`btn_regiP` `--blue-print`·`btn_scheP` `--orange`, `--white`, 148×24). 클릭 → `alert('연습 사이트에서는 지원하지 않습니다.')`. 비활성 회색 처리 금지. `step-7:PRINT_*` 는 SKIPPED 아님(버튼+alert 가 범위). `.timeT`/`.leftT` CSS 는 추후 시간표 화면 착수 시 활용.
- 근거: 사용자 결정 2026-09-06.

## D24 — 대기열 화면: 최후순위 이관, 미구현 (Q-6)
- 실제/가짜 대기열 중 미정. 다른 항목 전부 끝난 뒤 재논의. `step-7:WAITING_ROOM` 은 재논의 전 SKIPPED(사유 D24).
- 근거: 사용자 결정 2026-09-06.

## D25 — API 계약: mock 우선, 프론트가 Spec 을 도출, http 어댑터 착수 금지 (Q-1)
- 순서: 프론트 mock 완성 → 완성도 비교 → 프론트가 예측하는 API Spec 도출(`SukangApi` + 02 §2 모델) → 그 Spec 기준으로 서버 수정. mock 어댑터 인터페이스를 깔끔히 유지.
- 근거: 사용자 결정 2026-09-06 · 02 §8.

## D26 — 시드 = `intake/INU-시드데이터.md` 관측 74건 · 모델 정정 · 학점 상한 (Q-2·Q-14)
- 임의 픽스처 전량 교체. 교양 개설학과는 `교양`(일반선택은 `일선`), 교강사에 부서명 가능(창업지원단·취업경력개발원), 강의실은 실제 건물번호(05 07 08 09 12 13 14 16 29 + ZZ/ZC). 시드 `※부분` 항목만 §10 가이드로 보완(주석 표기).
- 모델: `schedule` 은 원문 문자열, `nameEn` 은 괄호 없음(표시 시 감쌈), `tags` 는 이름(표시 `[이름]`, HUSS 변형 2종 포함). 시드 필드명 대응 nameKo→name, credit→credits.
- 학점 상한: `Student.gpa`(UI 비노출, 시드 고정값 3.2) → 기본 20(기준 17~19, 부득이 20) · GPA 3.5↑ 21 · GPA 4.0↑ 24. 프론트는 "합계 > 상한 차단" 메시지만. 상한 테스트는 시드 값 변경으로.
- 근거: 사용자 결정 2026-09-06 · 시드 §0·§1·§8·§9.

## D27 — 원어여부 = `Course.isEnglish` → `EN(원어)` 표시 (Q-14)
- true → 셀에 `EN(원어)`, false → 빈 셀. 전체 5% 내외(시드 §5 관측 2건). 근거: 사용자 결정 2026-09-06.

## D28 — 이수구분 enum 8종 확정 (Q-14)
- `전공기초` `전공심화` `기초교양` `핵심교양` `심화교양` `일반선택` `교직` `군사학`. `courseType`·`resolvedType` 모두 z.enum. 근거: 사용자 결정 2026-09-06.

## D29 — 교양 select 값 = 02 §5-1 코드값 (Q-14)
- `cmbCptnGbn` 11/21/23/50/70/80 · `cmbFldGnb11` 161·162 · `cmbFldGnb21` 171~176 · `cmbFldGnb23` 182~186. `ㆍ` U+318D. 이름을 값으로 쓰지 않는다. 근거: 사용자 결정 2026-09-06.

## D30 — 타학과·연계전공 select 값 = 이름 (Q-14)
- 코드 미확보 → 이름이 곧 값(목록 02 §5-2/§5-3). 연계전공 관측 시드 없음 → mock 빈 결과. 근거: 사용자 결정 2026-09-06.

## D31 — `Student.status` = `유예`, 헤더 포맷 `{학년} / {학적상태}` (Q-14)
- 근거: 사용자 결정 2026-09-06.

## D32 — 세션 만료 → alert 후 로그인 화면 유지 · 서버 시간 동기화는 추후 별도 항목 (Q-13)
- 서버 시간 표시 UI 없음 유지. 동기화는 핵심 연습 요소라 나중에 별도 항목으로 다룸(지금은 유틸만, 미구현). 근거: 사용자 결정 2026-09-06.

## D33 — 원본 UX 결함 3건 그대로 재현 재확인 (Q-3, D4 보강)
- 신청/취소 후 조회 목록 미갱신(버튼 `신청` 유지) · 0건 안내 없음 · 정원/신청인원 비노출. 개선 금지. 스크린샷 재확인 완료. 근거: 사용자 결정 2026-09-06.

## D34 — CAPTCHA 로직 구현, `VITE_CAPTCHA=on|off` 기본 off (Q-4, D3 변경)
- 사용자 대면 토글 UI 없음. ON 이면 원§9.1(01 부록 A) 그대로: 신청 클릭마다 발동 · 4자리 숫자 + 노이즈(배경 주황/노랑 랜덤) · 정답 시 보류 요청 자동 수행 · 오답 시 모달 유지 + 하단 파란 굵은 글씨 에러 · 10회 소진 시 자동 로그아웃. `beforeSubmit` 훅(`useCaptchaGate`)으로 구조화. 시각 = 05 §10, 토큰 = 04 §1-2.
- 근거: 사용자 결정 2026-09-06.

## D35 — 초기 = 랜딩 상태 · 동작 기본값 유지 (Q-7·Q-8·Q-9, D20 대체)
- 로그인 직후: `>>` 제목 없음 · 전공과목 안내 3줄만 · 결과 테이블 영역 비어 있음(헤더도 없음) · 신청내역은 헤더만(초기 0건). 탭 클릭 시 진입. `?menu` 무효값도 랜딩.
- 유지: 조건 미충족 조회 무동작·메시지 없음 / 로그인 빈 값 진입 불가 / 찾기 링크·바로가기 렌더만.
- 근거: 사용자 결정 2026-09-06.

## D36 — 시각 결정 묶음 (Q-10·Q-11·Q-12·Q-15)
- 유지: 앱 명칭 `수강신청 연습 사이트 (비공식 Mock)` · 배너 최상단 1줄(설정 컨트롤 없음) · en 누락 컬럼 한글만 · 탭 활성 미구현(원본도 없음).
- **행 hover 구현**: `.dataT tbody tr:hover` 배경 `--row-hover #F5F8FD`(값 사용자 지정, 원 listColorOn/Off 대응).
- **웹폰트 필수**: `@fontsource/nanum-gothic`(OFL) 400/700 자체 호스팅, 스택 `Dotum, Gulim, 돋움, 굴림, 'Nanum Gothic', arial, sans-serif`. Arial 폴백은 재현 실패(D18 대체).
- **로그인 중앙 보정**: `#login` `margin:0; transform:translate(-50%,-50%); height:auto`(배너 높이 포함).
- 근거: 사용자 결정 2026-09-06.

## D37 — React 19 (Step 1 판단 승인)
## D38 — TypeScript 6 · ESLint 10 flat · 단일 tsconfig (승인)
## D39 — 리셋 `border-radius: var(--radius)` · Tailwind 기본 테두리색 `--border` (승인)
## D40 — Tailwind preflight 유지 + `.sch_areaT`(·`.sch_areaSubT`·`.leftT`) `box-sizing: content-box` 명시
- 원문이 미지정이라 높이 337px + 테두리 1/1/2px 기준이 달라지는 것을 되돌림. 근거: 사용자 결정 2026-09-06.
## D41 — 검증 순서 = 02 §4-1 (시간중복 → 동일과목명 → 학점 → 정원) 승인
- 시간중복·동일과목명은 독립(시간 안 겹쳐도 동일 과목명이면 차단, 실측). 시간표는 반교시 단위 비교(시드 §2.4 교차 배치 통과).
## D42 — 이수구분 파생 근사 승인 (시드 §8)
- 개설학과 == 소속 → 유지 / 전공 계열(전공기초·전공심화) → `일반선택` / 그 외(교양 등) → 유지. **실 규칙은 백엔드 소관(코드 주석 명시).**
## D43 — 초기 신청내역 0건 (조사 당시 실제 상태)
## D44 — `VITE_MOCK_FAIL=session` 은 신청/취소에만 적용 (승인)
## D45 — Loading = 데이터 없음 + fetching 일 때만, 재조회 중 기존 행 유지 (승인)
## D46 — Prettier 제외(`globals.css`·md) · 상수/프로바이더 선행 생성 (승인)
## D47 — 브랜치 `main` (master 커밋 이관 완료)
## D48 — 커밋 정책: 게이트별 자동 커밋 중단, 전 작업 완료 후 승인받아 일괄 커밋
- 그때까지 워킹 트리에 둔다. 리뷰 JSON 의 `commit` 은 `"pending(D48)"`. Co-Authored-By 트레일러 유지 가능. 근거: 사용자 결정 2026-09-06.
## D49 — 원격 push · `.env` 작성 · 원격 저장소 연결은 사람 몫 (유지)
## D50 — 원본 computed style 실측 대조 반영 (D-01~D-05, D36 일부 폐기)
- 원본(`sugang.inu.ac.kr`)과 구현물의 `getComputedStyle` 실측 대조 결과를 반영한다. **실측값이 명세보다 우선**(명세 3건은 결함으로 판명 → intake 명세 갱신 완료).
- **D-01 line-height**: 원본에 본문 `line-height` 규칙 없음(= `normal`). Tailwind preflight 의 `html,:host{line-height:1.5}` 를 `globals.css` 에서 `line-height:normal` 로 되돌린다. (행 높이 44.0→32.5px, 결과 영역 가시 행 7.7→10.4행)
- **D-02 폰트(D36 대체)**: 폰트 스택은 원§7 원문 `Dotum, Gulim, 돋움, 굴림, arial, sans-serif`. **`'Nanum Gothic'` 및 `@fontsource/nanum-gothic` 의존성 제거.** 근거: 측정 머신에 Dotum/Gulim 이 없어 원본이 시스템 기본 sans-serif 로 렌더되므로, 웹폰트를 얹으면 오히려 원본과 글자 폭이 ~10% 어긋난다(D36 의 "Arial 폴백 = 재현 실패" 전제가 실측으로 반증됨). 볼드가 더 굵어 보이던 현상도 이 서체 차이의 파생 효과였다. D36 의 나머지 항목(행 hover `--row-hover`, `#login` transform 중앙 보정, 앱 명칭·배너·en 라벨·탭 활성)은 유지.
- **D-03 perT `<colgroup>`**: 원본 마크업에 `<col>` 7개(`22/12/14/12/14/12/14%`)가 있으나 원§5.2 에 누락. `features/sukang/columns.ts` 의 `PER_TABLE_COL_WIDTHS` 로 복원(D10 준용 — 폭은 설정 데이터).
- **D-04 `*` 리셋**: 원본에 `*` 규칙 자체가 없고 선언은 `margin/padding/border` 3개뿐. `vertical-align:baseline` 제거(표 셀이 UA `table{vertical-align:middle}` 상속 회복). `font-size:100%` 는 현재 무해라 존치하되, `<font size>` 방식 화면이 추가되면 제거한다. `border-radius:var(--radius)`(D39)는 존치.
- **D-05 화면 제목**: `>> 화면명` 은 평문이 아니라 bold + `#5D7287` + 12px(9pt) + center — 원본 인라인 style. 토큰 `--slate-title` 신설, 클래스 `.tit_scr`. 조건 없는 화면은 `<table>` 에, 조건 있는 화면은 제목 `<td>` 에만 적용(같은 표의 검색 조건 오염 방지).
- ⚠️ `.claude/spec/` 은 phase=BUILD 에서 읽기전용이라 `04 §1-2`(토큰) · `05 §3-1/§3-3`(마크업)에 이 델타가 반영되지 않았다. **이 D50 항목이 해당 부분의 상위 권위**(`rules/source-of-truth.md` §2-1).
- 근거: 사용자 결정 2026-09-06(원본 실측 대조 계획서 D-01~D-05) · Nanum Gothic 완전 제거는 같은 날 추가 확인.
<!-- PROJECT:END -->

## 기타 확정값 (계약에서 고정 — 임의 변경 금지)
<!-- PROJECT:BEGIN — 고정 계약값 (근거: spec/01·02·04) -->
- 페이지네이션 없음 — 전건(원§1). 결과 영역 높이 337px 내부 스크롤(원§6).
- `.wrap` 80% / min-width 1400px · 본문 12px · 버튼 11px · 영문 라벨 10px · 헤더 14px · `border-radius: 0`(원§7). 폰트 스택 `Dotum, Gulim, 돋움, 굴림, arial, sans-serif`(D50 — 웹폰트 없음). 전역 `line-height: normal`(D50).
- `table.perT` `<colgroup>` = `22/12/14/12/14/12/14%`(D50). 표 셀 `vertical-align: middle`(UA 상속, D50). 화면 제목 `.tit_scr` = bold · `--slate-title` · 12px · center(D50).
- 과목명(코드) 조회 최소 **2자**(원§8.6) → `shared/constants/fieldLimits.ts`.
- 교양 트리 **2단 고정**(3단 없음) · 하위 select 3개 전부 렌더 + 표시 토글(원§4.1).
- 타학과 76 · 연계전공 32 · placeholder `=` 개수 원문(원§4).
- 메시지 포맷 `한글\n 영문`(공백 1칸) · 전부 native alert/confirm(원§9.2).
- 검증 순서 시간중복 → 동일과목명(독립) → 학점 → 정원(원§10.1, D41). 이수구분 = 파생값(조회/내역 상이 가능, 원§10.2) · enum 8종(D28) · 원어여부 `EN(원어)`(D27) · 학점 상한 gpa 기반 20/21/24(D26).
- 신청내역 순번 1부터 · 재수강 구분 신규 빈 값 · 신청/취소 후 조회목록 미갱신(원§10.3, D33) · 초기 신청내역 0건(D43).
- 서버 지연 200~800ms(원§12). 인증 없음 · 비밀번호 필드 금지(원§0).
- `--navy-900 #0F4EAB`(본문 헤더) ≠ `--login-navy #094A9A`(로그인) — 통일 금지(원§7).
<!-- PROJECT:END -->

관련: [[source-of-truth]] · [[api-contract]]

## D51 — 원본 computed style 2차 실측 대조 반영 (D2-01~D2-09)
- 2차 실측 계획서(원본 `sugang.inu.ac.kr` ↔ 구현물 `getComputedStyle` 대조, 5개 화면)의 결함 9건을 반영한다.
  **실측값이 명세보다 우선**(명세 4건 D2-01·D2-04·D2-08·D2-09 는 결함으로 판명 → intake 명세 갱신 완료).
- **D2-01 조회 전 안내 박스(렌더링 조건)**: 원본은 **조회 전에 `.sch_areaT` 와 결과 `table.dataT` 를 렌더하지 않는다**(실측 0개).
  그 자리에 안내 박스(1380×345 · 테두리 1px `--guide-border` · 본문 `--guide-bg` padding 6px · 콘텐츠 1360×320 ·
  `11pt` bold · `<br>` 줄 구성)가 오고, 조회 후 `.sch_areaT` + 결과 테이블로 **교체**된다.
  → 화면별 안내 문구(원§8.6)는 `GuideBox` 안에만 존재하며 **조회 후에는 사라진다**. 강조 줄은 원 `<font color>` 그대로
  순수 blue `--em-blue #0000FF`(교양 2줄) / red `--em-red #FF0000`(전공 1줄) — `--maroon` 아님.
  · 파생: 조건 없음 화면(장바구니·전공과목·HUSS)은 즉시 로드라 조회 전 상태가 없다 → **전공과목 안내는 랜딩(D35)에서만 보인다.**
  · `--bg-noti`·`.scr-noti`(하네스 안내 띠)는 폐기 — 토큰 정의만 존치.
- **D2-02 메뉴 탭 영문**: `span.tab-en` = 10px `--blue-en`(헤더 `.th-en` 과 동일 값).
- **D2-03 주의 문구**: 평문이 아니라 `<em>고동색→야간학과</em>` / `<span>회색→마감강좌</span>` 구간 — 원 CSS `.noti em`·`.noti span` 이 걸린다.
- **D2-04 조회 버튼**: `.btn_sch`(진회색+흰글씨)는 **원본 CSS 에 있으나 조회 버튼에 적용되지 않는 규칙**이다. 실측은 125×22 양각 버튼
  (`--btn-sch-bg` 배경 · `--slate-title` 글자 · 상·좌 `--btn-sch-bd-lt` / 우·하 `--btn-sch-bd-dk` · 내측 흰선 1px) → 전용 클래스 `.btn_search`.
- **D2-05 신청내역 제목**: `p.noti_blue` 가 아니라 `div.tit_sub`(14px · `padding-left:35px` · `margin:30px 0 15px` · 파란 화살표 불릿).
  한글 14/700, 영문 `span.tit-en` 13px `--blue-en`, 괄호 안내 14/400. 화살표는 학교 애셋 미사용(원§0) → 자체 제작 12×12 SVG data URI.
  원 CSS `.regi_area .regi_top p{float:right}` 는 출력 버튼 전용 → 제목은 그 위 전폭 블록.
- **D2-06 신청내역 헤더 영문**: 7 `Time Table(Lecture room)` · 8 `개설학과(부)` · 11 `Cancel`. **신청내역 표 전용 override**
  (조회 결과 표는 조회 전 렌더되지 않아 실측 대상이 아니었다 → 기존 라벨 유지, 추정 금지). D21 은 조회 결과 표에 한해 유지.
- **D2-07 신청내역 `colgroup`**: 순번 35 · 교과목명 **폭 미지정(가변)** · 재수강 구분 100. `<col>` 은 원본과 같이 `width` 속성으로 쓴다(D10 준용).
- **D2-08 폼 컨트롤**: 원본 5개 화면에 **`.sjt_sch` 가 존재하지 않는다** → 원§6 의 `.sjt_sch span input[type=text]`(200×32)·
  원§7 "검색 텍스트 입력 200×32" 는 **이 화면에 적용되지 않는 규칙**. 폼 컨트롤은 UA 기본(input 2px inset `#767676` / padding `1px 2px`,
  select 1px `#767676`). `size="30"` + 실측 고정 `--w-sch-input 185px` × `--h-sch-input 21px`(측정 머신 폰트 폴백에서 size 만으로는 193px).
- **D2-09 리셋(D-04 보강)**: 원본 리셋은 `*` 가 아니라 Meyer **요소 나열**이고 `input`/`select`/`textarea` 가 목록에 없다.
  `*{border:0}` 을 요소 나열로 바꾸고, Tailwind preflight 의 `*{border-width:0}`·`input,…{padding:0}` 은
  `input, select, textarea { border:revert; padding:revert; }` 로 되돌린다. `border-radius:var(--radius)`(D39)만 `*` 에 남긴다.
- 신규 토큰(이 결정으로 승인): `--guide-bg` `--guide-border` `--em-blue` `--em-red` `--btn-sch-bg` `--btn-sch-bd-lt` `--btn-sch-bd-dk`
  `--font-guide` `--font-tit-en` `--w-btn-search` `--h-btn-search` `--h-guide-inner` `--w-sch-input` `--h-sch-input`.
- ⚠️ `.claude/spec/` 은 phase=BUILD 에서 읽기전용이라 `04 §1-2`·`05 §3-2/§3-3/§3-4/§4-5/§5-3/§6` 에 이 델타가 반영되지 않았다.
  **이 D51 항목이 해당 부분의 상위 권위**(`rules/source-of-truth.md` §2-1). intake 명세는 §2.3·§5.8·§5.9(신설)·§6·§7·§8.3·§8.6·§8.7 갱신 완료.
- 🙋🏻 **미확인(추측 금지, TODO)**: ① 조회 결과 **0건** 화면(안내 박스 복귀 vs 빈 `.sch_areaT`) — 현행 D9 Empty(thead 만) 유지.
  ② 장바구니 조회 전 상태(원본 `.sch_areaT` 1개 관측 vs 스크린샷 안내 박스) — 현행 즉시 로드 유지. ③ `btn_red` 실제 행 폭(`width:90%`).
- 근거: 사용자 결정 2026-09-06(2차 수정 계획서 D2-01~D2-09) · 브라우저 실측 재확인 완료.

## D52 — 원본 computed style 3차 실측 대조 반영 (D3-01~D3-11)
- 3차 실측 계획서(원본 `sugang.inu.ac.kr` 교양과목 화면 ↔ 구현물 `getBoundingClientRect`/`getComputedStyle` 대조)의
  결함 11건을 반영한다. **실측값이 명세보다 우선.**
- **D3-01 탭 간격**: 실제 탭 간격은 `margin-right:1px` 이 아니라 **4.3px**(= 1px + 원본 HTML 소스 공백 텍스트 노드 ≈3.3px).
  JSX 는 그 공백을 없애므로 `.btn_re { margin-right:4.3px }` 로 직접 준다(`{' '}` 는 폰트 폴백에 따라 폭이 흔들림).
- **D3-02 배너 제거·문서 제목 변경**: 연습용 배너(`PracticeBanner` · `.practice-banner` · `PRACTICE_BANNER_TEXT`)를
  **제거**하고 `<title>` 을 `인천대학교 모의 수강신청` 으로 바꾼다. → **D11 폐기**, D12 는 로그인 화면 앱 명칭
  (`APP_TITLE` = `수강신청 연습 사이트 (비공식 Mock)`)·`[LOGO]` 플레이스홀더에 한해 유지되며 `<title>` 동일 조항만 해제.
  🙋🏻 로그인 화면 `<h2>` 문구는 이번 계획서 범위 밖이라 그대로 뒀다(변경 여부는 사용자 결정).
- **D3-03 검색행 구조**: 원본 검색행은 `<table>` 이고 요소 간격은 **`width="10px"` 스페이서 `<td>`** 가 만든다
  (padding/margin 아님). 좌측 들여쓰기 20px · 상단 10px · 행 높이 25px · 조회 버튼은 행 상단에서 2px 아래.
  · 검색행 `<table>` 은 원§8.4 의 인라인 style `font-weight:bold; font-size:9pt; font-family:굴림; text-align:center`
    를 그대로 갖는다. **`font-family:굴림`(폴백 스택 없음)이 폭의 원인**이다 — 제목 56.4 / `→` 10.4.
    body 스택으로 렌더하면 `arial` 이 U+2192 를 12px 로 그려 어긋난다(3차 §5.2 미확인 항목 규명 완료).
  · 조건 없는 화면(장바구니·전공과목·HUSS)도 같은 테이블이며 20px 들여쓰기는 빈 선두 `<td>`(원§8.4 폭 76.4 = 20 + 56.4)가
    만든다 → 구현물은 두 분기 모두 `.sjt_sch` 로 통일(세로 간격은 종전과 동일, 제목만 20px 우측 이동).
- **D3-04 안내 박스 여백**: 5px 흰 여백은 **테두리 바깥(margin-top)** 이고 테두리 안쪽 여백은 4방향 모두 3px.
  → `.guide_box { margin-top:5px; padding:3px; }`(자체 340px + margin 5 = 점유 345px).
- **D3-05 검색행 하단 여백 제거**: `.sjt_sch` 의 `margin-bottom:20px`·`padding-bottom:10px` 제거(`--sp-sch-mb`·`--sp-sch-pb` 미사용).
  안내 박스는 검색행 바로 아래에 붙고, 시각적 간격은 D3-04 의 margin 5px 가 담당한다.
- **D3-06 검색행 구분선 제거**: `.sjt_sch` 의 `border-bottom:1px solid --border-sch` 제거(원본 검색행 `<table>` 에 테두리 없음).
- **D3-07 select 가운데 정렬**: 원본 `select` 의 computed `text-align:center` 는 **검색행 테이블에서 상속된 값**이다
  (D3-03). Chrome UA 가 select 의 text-align 을 자체 값으로 덮으므로 `.sjt_sch select { text-align:center }` 로 명시한다.
- **D3-08 주의 문구 위치**: `.noti` 는 우측 끝 정렬이 아니라 **마지막 탭 바로 뒤 인라인**(간격 4.3px).
  `.btn_area` 의 flex/justify-between 을 걷어내고 원 CSS 그대로의 블록 + 인라인 흐름으로 되돌린다.
  · 파생: 3차 §5.1 의 **탭 높이 1.5px 차이(38.5 vs 37)는 이 flex 컨테이너가 원인**이었다 —
    블록으로 되돌리자 37px 로 일치. `height` 하드코딩 불필요(미확인 항목 규명 완료).
- **D3-09 강좌 행 hover**: 원본은 CSS `:hover` 가 아니라 행마다 `onmouseover=listColorOn(this)` 인라인 핸들러로
  `#e1f1ff` 를 칠한다(마감 행 `tr.grey` 포함, 예외 없음). `--row-hover` 를 `#F5F8FD` → **`#E1F1FF`** 로 정정하고
  셀렉터를 `.sch_areaT table.dataT tbody tr:hover` 로 좁힌다(신청내역 표는 원본 관측 대상 아님). → D36 의 hover 항목 대체.
- **D3-10 `.leftT` 래퍼**: 신청내역 테이블을 `div.leftT` 로 감싼다(원§6 규칙 — 명세에서 누락됐고 시간표 화면 전용이 아니다).
  실측 1382×323 = 내용 1380×320 + 좌·우 1px + 하단 2px(`box-sizing:content-box`, D40 준용).
- **D3-11 출력 버튼 위치**: 출력 버튼 묶음은 별도 행(`.regi_top`)이 아니라 **`.tit_sub` 안의 `div.tit_btns{float:right}`** 다.
  float 는 부모 높이에 기여하지 않아 `.tit_sub` 는 16.5px 한 줄을 유지하고, `margin-bottom:15px` + `.leftT` 테두리 1px =
  제목 하단 → 테이블 상단 **16px**(종전 43.5px). → **`.tit_sub` 에 clearfix 금지**, clearfix 는 `.regi_area` 자신에만 건다
  (`.regi_area > div::after` → `.regi_area::after`). `.regi_top` 요소·CSS 는 제거(`--sp-regi-mb` 미사용).
- 신규 토큰 없음. 값 변경 1건(`--row-hover`). 미사용이 된 토큰(`--sp-sch-mb` `--sp-sch-pb` `--sp-regi-mb` `--border-sch`)은 정의만 존치.
- ⚠️ `.claude/spec/` 은 phase=BUILD 에서 읽기전용이라 `04 §1-2`(토큰) · `05 §1/§3-2/§3-3/§6` 에 이 델타가 반영되지 않았다.
  **이 D52 항목이 해당 부분의 상위 권위**(`rules/source-of-truth.md` §2-1). intake 명세는 §5.9·§5.10(신설)·§5.11(신설)·
  §6·§7·§8.3·§8.4·§8.8(신설) 갱신 완료.
- 🙋🏻 **미확인(추측 금지, TODO)**: D51 이월 3건(① 조회 결과 0건 화면 ② 장바구니 조회 전 상태 ③ `btn_red` 실제 행 폭)은
  여전히 미확인 — 현행 유지. 3차에서 새로 규명된 §5.1(탭 높이)·§5.2(select 정렬)는 위 D3-08·D3-03 으로 해소.
- 🙋🏻 **범위 밖 잔여 편차(3차 계획서 미포함, 보고만)**: ① 세로 좌표가 전 구간 +1.5px(원본 `.btn_area` top 71.5 vs 73)
  — `table.perT` 높이 53.5 vs 55 에서 비롯. ② 출력 버튼 묶음 폭 304.1 vs 원본 287.8(우측 끝은 일치).
- 근거: 사용자 결정 2026-09-06(3차 수정 계획서 D3-01~D3-11) · Playwright `getBoundingClientRect` 실측 재확인 완료.

## D53 — 원본 computed style 4차 실측 대조 반영 (D4-01~D4-03)
- 4차 실측 계획서(원본 `sugang.inu.ac.kr` 연계전공·과목명(코드)조회 화면 ↔ 구현물 `Range`/`getComputedStyle` 대조)의
  결함 3건을 반영한다. **실측값이 명세보다 우선.** 안내 박스 좌표는 두 기준이 섞이지 않도록 **테두리 좌상단**을 원점으로 환산해 비교한다
  (원본은 5px 흰 여백이 요소 rect 안, 구현물은 `margin-top` 으로 rect 밖).
- **D4-01 안내 문구 좌측 들여쓰기**: 원본 텍스트 노드는 각 줄이 개행·탭에 이어 **THIN SPACE(U+2009)** 로 시작해
  하늘색 영역 좌측에서 **20.66px** 들여쓰기된다(테두리 좌상단 기준 글자 시작 x = 1 + 3 + 20.66 = **24.66**, 구현물은 10.00).
  THIN SPACE 의 실폭이 폰트 폴백에 좌우되므로 마크업이 아니라 CSS 로 재현한다 →
  `.guide_box .guide_inner { padding: 6px 6px 6px 20.66px }`. **하늘색 배경 범위(테두리에서 4px)와 세로 위치는 손대지 않는다.**
- **D4-02 불릿 문자**: `▤`(U+25A4) → **`▒`(U+2592 MEDIUM SHADE)**. 5개 화면 전부 = `constants/notices.ts` 한 곳.
  스모크(`.claude/resource/smoke/sukang.spec.ts`)의 `em_red` 단언 문자열도 함께 정정.
- **D4-03 검색 입력 치수**: 원본 입력에는 인라인 style 도 전용 CSS 규칙도 **없다** — 폭은 `size="30"`,
  높이는 폰트 메트릭으로 브라우저가 자동 계산하고 `box-sizing` 은 `content-box`(원본에 `*` 규칙이 없다). 실측 **185 × 20.5**.
  → D51 D2-08 의 고정 치수를 걷어내고 `.sjt_sch input[type="text"] { box-sizing:content-box; width:auto; height:auto }` 로 바꾼다.
  테두리·padding·배경은 UA 기본이므로 선언하지 않는다. 쓰이지 않게 된 토큰 `--w-sch-input`·`--h-sch-input` 삭제
  (`--w-search-input` 200px 은 CAPTCHA 입력 전용으로 별개 — 존치).
  ⚠️ **preflight 의 `*{box-sizing:border-box}` 를 전역으로 되돌리지 않는다.** 1~3차에서 `.sch_areaT`(337px)·
  `.guide_box`(padding 3px)·`.btn_search`(125×22)·`.btn_blue` 등을 border-box 전제로 맞춰 놓았고, 원본도 일부 요소는
  border-box 로 계산되므로 전역 통일이 정답이 아니다. **요소별 개별 대응**이 원칙.
- **서체 미수정 결정(4차 §6.1)**: 원본 안내 박스는 전역 스택이 아니라 `font-family:굴림` 단독 인라인 지정이다.
  구현물의 상속값은 body 스택의 `arial` 이 한글 폴백을 가로챈 결과(1행 글자 폭 271.55 = arial 측정값과 소수점까지 일치).
  **수정하지 않기로 결정** → 줄 높이 −1.0px(17.5 vs 16.5)·1행 글자 폭 +4.19px(+1.6%) 편차가 남는다. 검증 시 실패로 판정하지 않는다.
  되돌리려면 `.guide_box { font-family: 굴림 }` 한 줄.
- 신규 토큰 없음. 삭제 2건(`--w-sch-input` `--h-sch-input`).
- ⚠️ `.claude/spec/` 은 phase=BUILD 에서 읽기전용이라 `05 §3-4`(안내 문구 불릿)·`04 §1-2`(토큰)에 이 델타가 반영되지 않았다.
  **이 D53 항목이 해당 부분의 상위 권위**(`rules/source-of-truth.md` §2-1). intake 명세는 §5.9·§6·§7·§8.6 갱신 완료.
- 🙋🏻 **검증 환경 편차(헤드리스 Chromium 실측, 사용자 브라우저 재확인 필요)**: `Dotum`/`Gulim` 이 없는 측정 머신에서
  `size="30"` 의 자동 폭이 계획서 기대치(185)와 다르게 나온다(폴백이 Times New Roman 이면 193, sans-serif 면 202 —
  프로브 확인). 원본과 **같은 규칙**을 쓰므로 두 문서가 같은 브라우저에서는 함께 움직이지만, 절대값 검증은 사용자 브라우저에서 해야 한다.
- 🙋🏻 **미확인(이월)**: D51/D52 이월 3건(① 조회 결과 0건 화면 ② 장바구니 조회 전 상태 ③ `btn_red` 실제 행 폭) ·
  D52 이월 2건(④ 메뉴 탭 높이 37 vs 38.5 ⑤ 원본 `select` `text-align:center` 출처 — D52 D3-03/D3-08 로 규명했으나 4차에서 재제기) ·
  ⑥ 전역 `box-sizing` 차이의 요소별 영향 범위(4차 §6.2, 차기 조사).
- 근거: 사용자 결정 2026-09-06(4차 수정 계획서 D4-01~D4-03) · Playwright 실측 재확인 완료.

## D54 — 원본 computed style 5차 실측 대조 반영 (D5-01~D5-06) · 로그인/CAPTCHA
- 5차 실측 계획서(원본 `sugang.inu.ac.kr` **CAPTCHA 모달 · 로그인 화면** ↔ 구현물 `getComputedStyle`/`getBoundingClientRect` 대조)를
  반영한다. **실측값이 명세보다 우선.** 이 결정으로 **D11(폐기 상태 유지) · D12 · D3/D34 일부 · D36 로그인 항목**이 정리된다.
- **D5-01 로그인 제목 = 원본 문구 복원 (D12 대체)**: `.tit` 은 h1 하나뿐이다 —
  `<font>인천대학교</font>`(48px bold **`--black` 순수 검정**) + 6.7px 공백 + `<font>대학 수강신청</font>`(48px bold `--login-title #16529B`)
  + `<br>` + `Undergraduate Course Registration`(h1 자체 24px bold `--text`). `LOGIN` 은 `h3 > font` 48px `--white`.
  → **`[LOGO]` 플레이스홀더·`APP_TITLE`(h2)·`shared/constants/appText.ts` 제거.** D12 의 "학교 CI·인천대학교 표기 금지" 는
  이 결정으로 해제된다(`<title>` 은 D52 D3-02 에서 이미 `인천대학교 모의 수강신청`). 학교 **이미지 애셋은 여전히 사용하지 않는다**(원§0) — 전부 텍스트다.
  · 근거: 사용자 결정 2026-09-07(질문 1 "원문 그대로 복원").
- **D5-02 비밀번호 입력 행 복원 — `input[type="password"]` 금지 규칙 해제**: `.id_w` 2행(`비밀번호 (PW)` 라벨 110×32 + 입력 184×32)을
  원본대로 되살린다. 행을 빼면 `.btn_login` 72px(=32+8+32)·`.id_w` 143px 근거가 무너진다.
  · **값은 읽지도 저장하지도 전송하지도 않는다** — RHF 에 register 하지 않는 순수 시각 재현 필드다. 인증은 여전히 없다(D14).
  · `01 §1 필수 1`·`rules/api-contract.md`·`rules/ui-conventions.md` 의 "비밀번호 필드 금지"는 **로그인 화면에 한해** 해제.
    수강신청 화면에는 여전히 존재하지 않는다(스모크 1 이 계속 단언).
  · ⚠️ 하네스는 이 변경에 반대 의견을 냈다(실제 포털과 같은 모습으로 비밀번호를 받는 화면이 된다). 사용자가 재확인해 진행했다.
  · 근거: 사용자 결정 2026-09-07(질문 2 `type="password"` 로 원본 동일).
- **D5-03 입력 필드 배경 없음**: 원본 CSS 에 배경 지정이 없다(실측 `#E8F0FE` 는 Chrome 자동완성 하이라이트) → UA 기본 흰색.
  `--login-input(#E0E0E0)` 은 **미사용**이 되며 정의만 존치. 폰트는 실측대로 `Dotum, Gulim` 12px 를 두 입력 타입에 함께 준다
  (전역 리셋의 `input[type="text"], select, textarea` 는 `password` 를 덮지 못한다).
- **D5-04 `#login` 중앙 정렬 원문 복귀 (D36 잔여 항목 폐기)**: 배너가 사라졌으므로(D52 D3-02) transform 보정을 걷고
  원문 `margin:-190px 0 0 -464px; height:380px` 로 되돌린다(토큰 계산 `calc(var(--h-login) / -2)`). 안내 문구는 박스 아래로 **넘쳐 흐른다**(원본 동일).
- **D5-05 파란 박스 아래 안내 문구 — 1·3행만**: x 기준선 293(`.login_con` 좌측 + padding 60) · 12px/400 · 줄 간격 28.5px(줄 높이 13.5 + 15).
  1행 `※ 수강신청 URL https://sugang.inu.ac.kr`(`--em-blue`, `<a>` 안) · 3행 `※ 호환성 문제에 따라 …(Safari 등 사용 불가)`(`--em-red`).
  **2행(모의 수강신청 INTIP)은 삭제** → 3행이 y=697.5 로 올라온다. 링크는 `href` 없이 렌더만(D19).
- **D5-06 전역 `a:hover { color: --login-hint }`**: 원본은 `style.css` 의 `a:hover{color:#333}` 뒤에 인라인 시트가 같은 선택자로 한 번 더
  선언한다(명시도 동일·`!important` 없음 → 뒤가 이긴다). **전역 적용**이므로 `.btn_login`·`.btn_etc*` 뿐 아니라
  **수강신청 화면 메뉴 탭(`a.btn_re`)까지** hover 시 글자색이 옅어진다 — 원본 동작 그대로 재현한다.
  · 자식이 자기 색을 직접 지정한 경우(안내 문구 `span.em_blue`)는 상속이 아니라 자기 선언이 이기므로 hover 해도 파랑을 유지한다
    (5차 §3.2 표의 해당 줄은 캐스케이드상 성립하지 않는다 — 구조 충실 쪽을 택했다).
  · 로그인 화면 한정으로 되돌리려면 셀렉터를 `#login a:hover` 로 좁히면 된다(한 줄).
- **CAPTCHA (D34 구현 상세 확정)**: **jquery-confirm 3.3.4 를 실제 설치**하고 원본과 같은 클래스 구조
  (`jconfirm jconfirm-light jconfirm-open` / `jconfirm-box jconfirm-hilight-shake` / `-title-c` `-content-pane` `-content` `-buttons`)로 구성한다.
  · **CSS 만 import 한다**(`main.tsx`, `globals.css` 보다 먼저). 모달 DOM·상태는 React 가 소유하므로 **jQuery 런타임은 번들에 들어가지 않는다**
    (`jquery` 는 `jquery-confirm` 의 의존성으로 설치만 됨). 벤더 CSS 가 실측값을 그대로 만든다:
    박스 374 = 15 + 35 + 262 + 15 + 36 + 11 · 오버레이 `#444`/.2 · radius 5px · `닫기`(`.btn-blue` `#3498DB`/흰색/14px bold).
  · 벤더 기본값 중 2가지만 덧쓴다(둘 다 폰트 폴백에서만 어긋난다): `.jconfirm-title-c { height:35px }`(제목 `vertical-align:middle`
    인라인 박스가 line-height 를 넘김) · `.jconfirm-content-pane { display:block }`(inline-block 베이스라인 갭 3px 제거 → 총 374 일치).
  · 본문은 학교 마크업(`.capt_box` 699×261 `--capt-box-bd` / 이미지·본문 2열 table / `.capt_field` / `.capt_err`).
    `확인(Confirm)` = 기존 `.btn_mrc` 재사용(색 조합이 `.btn_blue` 와 동일). 입력창 `size="20"` `maxlength="4"` `--capt-input-bg`,
    폭·높이는 **고정하지 않는다**(D53 D4-03 준용 — 원본과 같은 규칙이어야 환경이 달라져도 함께 움직인다).
  · **이미지**: `<canvas>` 210×210 · 숫자 1000~9999 · 글자 `--black` · 노이즈 베지어 40개 `--capt-line` ·
    배경 4색 로테이션(`--capt-bg-1..4` = 주황/연두/청록/하늘). D34 의 근사 토큰(`--captcha-bg-orange/yellow`·`--captcha-overlay`·`--w-captcha`)은 폐기.
  · **오답(§1.8)**: 모달·크기 유지 · 이미지 재생성 · 입력창 비움 · 입력 행 아래 2줄(`--em-blue` bold, 줄 간격 16px, 중복 누적 없음).
    **`Remain` 은 오답 1회당 정확히 1 감소**(명세의 `10 → 8` 관측은 오기 — intake §9.1 정정). 0 도달 = 자동 로그아웃(→ `/`).
    카운터는 세션 유지, 정답 시에도 초기화하지 않는다.
  · 본문 강조: `"확인"` = `--em-blue`, 횟수(`10`·Remain 양쪽) = `--em-red`. 어느 `10` 까지 빨강인지는 명세가 구간을 특정하지 않아
    **모든 횟수 숫자**에 적용했다(판단 — 되돌리려면 `captcha/texts.ts` 세그먼트만 수정).
- 신규 토큰: `--black` `--login-title` `--font-login-logo` `--font-login-h1` `--font-capt-title`
  `--capt-box-bd` `--capt-input-bg` `--capt-line` `--capt-bg-1..4` `--w-capt-box` `--w-capt-inner` `--h-capt-inner` `--w-capt-img` `--lh-capt-err`.
  삭제: `--captcha-bg-orange` `--captcha-bg-yellow` `--captcha-overlay` `--w-captcha`.
- ⚠️ `.claude/spec/` 은 phase=BUILD 에서 읽기전용이라 `04 §1-2`(토큰) · `05 §2`(로그인) · `05 §10`(CAPTCHA) · `01 §1`(비밀번호 금지)에
  이 델타가 반영되지 않았다. **이 D54 항목이 해당 부분의 상위 권위**(`rules/source-of-truth.md` §2-1).
  intake 명세는 §5.12·§5.13(신설)·§7·§8.1·§8.9(신설)·§9.1·§9.1a(신설) 갱신 완료.
- 🙋🏻 **검증 환경 편차**(헤드리스 Chromium, Dotum/Gulim 없음 — 사용자 브라우저 재확인 필요): 한글 폴백 글꼴의 줄 높이가 원본보다 커서
  ① 안내 문구 줄 간격 30 vs 28.5 ② `.txt` 링크 사이 간격 9.1 vs 6.1(` / ` 3글자 폭) ③ CAPTCHA 입력창 `size="20"` 폭 133 vs 128 ·
  높이 24 vs 21 ④ 그에 따른 입력 행 y 241.6 vs 240.6 · 오답 문구 y 278.6 vs ≈275. **규칙은 원본과 같으므로 같은 브라우저에서는 함께 움직인다.**
  좌표가 정확히 일치한 항목: `#login`·`.tit`·`.login_con`·`.login_area`·`.id_w`·두 입력 행·`.btn_login`·바로가기 4종·안내 1행 y,
  CAPTCHA 박스 730×374·title-c 35·content 700×262·안쪽 박스 699×261·이미지 (33.1,76,210,210)·`확인` 폭 76.5·`닫기`(664.8,327,48.2,32).
- 🙋🏻 **미확인(이월 + 신규)**: D51~D53 이월 6건 · ⑦ 오답 문구 정확 좌표(스크린샷 역산 ±3px) · ⑧ 노이즈 곡선 개수·굵기·글자 회전 폭 ·
  ⑨ CAPTCHA 주황 배경 채도(`#ffd38d` vs 관측 스크린샷) · ⑩ 로그인 실패 시 화면 · ⑪ 정답 시 Remain 초기화 여부.
- 🙋🏻 **범위 밖 관찰(보고만)**: Tailwind preflight 의 `input{font-weight:inherit}` 때문에 폼 컨트롤이 부모의 bold 를 물려받는다.
  CAPTCHA 입력창은 이번에 `font-weight:normal` 로 되돌렸지만, **검색행 입력(`.sjt_sch table{font-weight:bold}` 상속)도 같은 상태**다 —
  원본은 UA 기본(normal)이므로 차기 회차에서 `input, select, textarea { font-weight:revert }` 로 일괄 정리할 후보다(D53 D4-03 폭에 영향 가능).
- 근거: 사용자 결정 2026-09-07(5차 수정 계획서 + 질문 3건 답변) · Playwright 실측 재확인 완료.

## D55 — 서버 API 계약 확정 · http 어댑터 연동 (2026-09-07)
- 프론트가 mock 에서 도출한 계약(`API-SPEC.md`)을 서버가 대조·확정했고(서버 명세 v1 + 인수인계 14건),
  그 델타를 프론트에 반영했다. **D25(mock 우선 → 프론트가 Spec 도출 → 서버가 맞춘다)의 종착점**이다.
  서버에 되돌린 요청 3건은 `SERVER-REQUESTS.md`(R-1~R-3).
- **D55-1 인증 도입 (D14 개정)**: 서버가 JWT 를 유지한다(학번 unique 제약 + 조작 방지). 로그인은 **학번 + 비밀번호**
  (사용자 결정 — 학번 소유 증명은 넣지 않는다. 이 사이트에서 학번은 권한이 아니라 표시값이고, 도용은 회원가입 단계
  "이미 사용 중인 학번" 안내로 처리한다). `access-token` 헤더에 토큰 값만(`Bearer` 없음). 401 → `/auth/re-issue`
  **단일비행 재발급** 후 원요청 1회 재시도, 재발급 실패 시 `SESSION_EXPIRED`.
  → **D54 D5-02 개정**: 비밀번호 필드는 더 이상 "시각 재현 전용"이 아니다. RHF 에 등록하고 실제로 전송한다.
    단 mock 어댑터에는 인증이 없어 비밀번호를 보지 않는다(학번만으로 진입 — 스모크·로컬 연습 유지).
  → 로그인 실패 alert 문구 신설(원본 실패 화면 미확인 D54 ⑩): `학번 또는 비밀번호가 올바르지 않습니다.\n Invalid ID or password.`
  ⚠️ 서버 v1 은 아직 `{ email, password }` 다. 프론트는 목표 계약 `{ studentId, password }` 로 구현했고 R-2 로 변경 요청 중.
- **D55-2 이수구분 9종 (D28 의 8종 대체)**: `전공핵심` 추가. 서버 적재분 2,436건 중 385건이라 enum 에 없으면
  전공 탭이 통째로 파싱 실패한다. 서버는 연계 API 원문을 그대로 저장하므로 값은 고정. 전공 계열(`MAJOR_COURSE_TYPES`)에도 포함.
- **D55-3 에러 카탈로그 개정 (사용자 승인)**: 원본에 대응이 있는 서버 실패는 **원문 문구로 매핑**한다 —
  `REG-003`(같은 강의 재신청) → `DUP_SUBJECT`(같은 강의는 과목명도 같다) · `CRS-004`(폐강) → `CLASS_FULL`.
  원본에 대응이 없는 3종만 신규 작성: `COURSE_TYPE_LIMIT`(CRS-006) · `NOT_REGISTERED`(REG-004) · `CANCEL_FAILED`(REG-005).
  `CART-004` 는 담기/빼기를 붙이지 않아 호출 경로가 없어 생략. **`NOT_IN_PERIOD` 제거**(서버가 수강신청 기간 도메인을
  만들지 않아 던지는 경로가 없다). `TIMEOUT` 은 서버가 던지지 않고 클라이언트 타임아웃(10초)에서만 난다.
- **D55-4 select 목록 (D30 보강)**: 드롭다운에 **보이는** 목록은 원본 실측값 그대로(타학과 76 · 연계전공 32) 유지한다.
  서버 목록 API(`/courses/departments` 62 · `/courses/interdisciplinary-majors` 11)는 **이름 → 코드 매핑표로만** 쓴다
  (요청 파라미터를 이름 대신 코드로 보내기 위함). 코드가 없는 항목을 고르면 요청을 보내지 않고 빈 결과 = 원본 0건 화면.
  전건 코드 부여는 R-1 로 요청 중(강의가 나중에 추가돼도 데이터만 적재하면 되도록).
- **D55-5 장바구니**: 서버에 담기/빼기 API 가 있으나 **붙이지 않는다**(원본에 없는 UI 를 만들지 않는다).
  조회만 하므로 계정 장바구니가 비어 있으면 빈 목록이다.
- **D55-6 지연·실패 시뮬레이션 (D1/원§12 개정)**: 서버는 200~800ms 인위적 지연도, 확률적 세션 만료·타임아웃도 넣지 않는다
  (대용량 조회·신청 성능이 서버 제품의 핵심이라 측정 지표가 오염된다). **mock 어댑터의 지연은 연습용으로 유지**한다.
- **D55-7 표시 보정**: 프로필 `grade` 가 `4학년` 으로 오는데 원본 표기는 `4 / 유예` 라 **`학년` 접미를 떼어** 표시한다.
  `academicStatus`(`재학`)는 그대로. 검색어는 서버 상한에 맞춰 **2자 이상 70자 이하**일 때만 요청한다(초과도 no-op, D19).
- **D55-8 경계 유지**: 서버 형태와 프론트 모델의 차이는 **전부 `features/sukang/api/httpApi.ts` 에서 흡수**한다 —
  응답 언랩 · 파라미터 이름 · `courseId` 파생(`courseResponse.id`) · 프로필 → `Student` 매핑 · 에러 코드 매핑.
  **화면·훅·컬럼 정의는 이번 연동으로 한 줄도 바뀌지 않았다.** mock 어댑터는 그대로 두어 `VITE_API_ADAPTER` 로 되돌릴 수 있다.
  `VITE_API_BASE_URL` 은 서버 기본 경로 `/api/v1` 까지 포함해 적는다.
- **D55-9 교강사(`professor`) 빈 값 확정 (사용자 결정 2026-09-07)**: 서버가 항상 `""` 를 준다(연계 API 원천에 교강사 없음).
  **다른 원천을 찾지 않고 빈칸 그대로 렌더한다** — 서버 요청에서 철회했다(R-3 이었던 항목).
  단 **교강사 컬럼 폭 80px 은 고정 유지**한다: `table-layout:fixed` + `<colgroup>` 이라 내용과 무관하게 고정되고,
  컬럼을 빼면 폭 합(교과목명 = 1378 − 나머지)이 어긋나 전 컬럼이 밀린다. 브라우저 실측으로 값 유무와 무관하게 80px 확인했고,
  **스모크 12** 가 이 폭을 단언한다(회귀 방지).
- 🙋🏻 **미확인(서버 회신 대기)**: ① `DELETE /registration/{courseId}` 의 키가 `Course.id` 와 동일한지(R-3).
  ② 서버 정렬(학년·이수구분·학수번호)이 원본 정렬과 같은지 — 원본 실측 미수행.
  ③ 실서버 대상 통합 검증 미수행(`.env` 작성·서버 기동은 사람 몫, D49).
- 근거: 사용자 결정 2026-09-07(인증 방식·select 목록·장바구니·에러 문구 승인) · 서버 API 명세 v1(PR #121, 이슈 #120).

## D56 — 서버 2차 회신 대응 (2026-09-07) · 조회 단위·유예·REG-003 순서
- 서버가 R-1(전제 정정 후 수용) · R-2(수용) · R-3(확인 완료)에 회신하며 Q1~Q5 를 물었다. 답변은 `SERVER-REQUESTS.md §4`.
  **이 회신으로 프론트 코드는 바뀌지 않는다** — 전부 서버 데이터·조회 단위 문제이거나 어댑터가 흡수한다.
- **D56-1 타학과 조회 = 개설학과 단위 (Q1)**: 학부를 골라도 하위 전공이 딸려오지 않는 방향으로 서버가 바꾼다.
  근거 ① 원본 드롭다운이 학부와 하위 전공을 **각각 별도 항목**으로 갖는다(명세 §4.2 "학부·전공·학과 혼재, 평면 리스트" —
  전자공학부/전자공학전공/반도체융합전공 등 6쌍 이상). ② 강의의 개설학과 값이 하위 단위로 찍힌다(시드 §6 HUSS).
  ③ 프론트 mock 도 개설학과 완전일치다.
  ⚠️ **직접 관측은 아니다** — 원본에서 학부를 골랐을 때의 결과를 보지 않았다(타학과 실측은 경영학부 1건, 그 화면엔 개설학과 컬럼이 없다).
  반증이 나오면 서버만 되돌리면 된다(프론트 무변경). **원본 재확인은 TODO.**
- **D56-2 HUSS 3종은 학과(부) (Q2)**: `HUSS(교류대학)`·`HUSS(타대학)`·`HUSS포용사회이니셔티브학부` 는 원본 `cmbTagwaCd` 76개에
  들어 있고 `cmbYungaeCd` 32개에는 없다(명세 §4.2/§4.3). 시드 §6 의 개설학과 값도 이 이름들이다 → 서버가 연계전공에서 학과로 옮긴다.
- **D56-3 `academicStatus` 에 `유예` 추가 요청 (Q3)**: 원본 perT DOM 실측이 `4 / 유예`(명세 §5.2). 서버는 재학/휴학 2종뿐이었다.
  프론트는 `status` 를 자유 문자열로 다뤄 **변경 없음**(D31 유지) — 서버가 저장만 하면 된다.
- **D56-4 `REG-003` 은 검사 순서를 뒤로 (Q4, D55-3 정정)**: 원본에는 '이미 신청한 과목' 개념이 **없다** —
  검증 순서(명세 §10.1)에 그 검사가 없고 메시지 카탈로그(§9.2)에도 문구가 없다. 원본에서 같은 강의를 재신청하면
  **시간표 중복이 먼저 걸려 `Duplicated time table.`**, 온라인 강좌만 `Duplicated Subject.` 가 뜬다
  (관측된 순서에서 따라 나오는 결론 — 재신청을 직접 시도한 관측은 없다).
  → 서버에 **REG-003 검사를 시간표 중복·동일 과목명 뒤로 옮겨 달라**고 요청. 반영되면 REG-003 은 도달 불가가 되고
  mock·서버·원본의 동작이 일치한다. 그때까지 `httpApi.ts` 의 `REG-003 → DUP_SUBJECT` 매핑은 안전망으로 존치한다.
  거절되면 차선책은 강의 `schedule` 유무로 갈라 매핑(권장하지 않음 — 프론트가 서버 의미를 되짚는 형태).
- **D56-5 학번 중복 확인 API (Q5)**: 회원가입 화면이 없어 지금은 불필요. 회원가입 착수 시 필요하므로 서버가 만드는 김에
  이메일 판과 대칭으로 넣어 두기를 요청(우선순위 낮음). `MEM-005` 는 **코드만** 받는다 — 문구는 프론트 소유(D6 포맷).
- **D56-6 학과·연계전공 2단계 적재 동의 (R-1)**: 프론트가 쓰는 값은 목록 API 의 `code`(내부 상수명)뿐이고 학사 코드는 보지 않는다
  (1차 문서의 "코드 부여" 표현이 부정확했다). 1단계(이름+상수명 등록 · 개설학과 단위 조회 · 전건 반환)만 끝나면 프론트는 완성이다.
  학사 코드 미충족으로 신설 24건이 빈 배열로 오는 동안에도 화면은 원본의 0건 상태(헤더만)와 같아 문제없다.
  `무역학부`(주간)는 원본 드롭다운에 없다 — 제외가 맞다.
- 🙋🏻 **미확인**: ① 원본에서 학부 선택 시 하위 전공 포함 여부(D56-1) ② 서버 정렬이 원본 정렬과 같은지 ③ 실서버 통합 검증(D49).
- 근거: 서버 2차 회신 2026-09-07 · 인테이크 명세 §4.2·§4.3·§5.2·§9.2·§10.1 · 시드 §6 재확인.

## D57 — `VITE_*` 환경변수 폐지 · 배포값은 코드 소유 · 배포 = mock + CAPTCHA on (2026-09-21)
- 보안 정책상 `VITE_` 접두사 환경변수를 쓸 수 없다(값이 공개 번들에 박히는 경로). `envPrefix` 로 이름만 바꾸는 우회는 **하지 않는다** — 노출은 같고 검사만 피한다.
- **배포값 = `src/shared/config/env.ts` 스키마 기본값**: `API_ADAPTER=mock` · `CAPTCHA=on`(D34 의 "기본 off" 를 배포에 한해 대체) · `MOCK_FAIL` 없음. **Vercel 환경변수 0개.**
- 로컬 dev 서버(`vite serve`)에서만 `.env` 의 **접두사 없는 키**(`API_ADAPTER`·`API_BASE_URL`·`MOCK_FAIL`·`CAPTCHA`)로 덮어쓴다. `vite.config.ts` 가 Node 쪽에서 읽어 `__DEV_OVERRIDES__` 로 주입하고, 프로덕션 빌드는 읽지 않는다(빈 객체). 스모크는 `playwright.config.ts` `webServer.env` 로 `CAPTCHA=off`.
- `vercel.json` SPA fallback rewrite(`/(.*)` → `/index.html`) — `/sukang` 새로고침·직접 진입 404 방지.
- 🙋🏻 **서버 연동 시(추후)**: `API_BASE_URL` 을 번들에 넣지 않도록 **Vercel rewrite 프록시**(`/api/v1/:path*` → 서버)로 전환하고 코드는 같은 도메인 `/api/v1` 고정 — D55-8 의 `VITE_API_BASE_URL` 조항을 그때 대체한다. 지금은 mock 이라 미착수.
- 근거: 사용자 결정 2026-09-21(보안 정책 · 배포 mock · CAPTCHA on).
